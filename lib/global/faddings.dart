import 'package:flutter/material.dart';

class GlobalFaddings {
  final base = const EdgeInsets.only(
    left: 0.04,
    right: 0.04,
    top: 0.01,
    bottom: 0.01,
  );
  final sides = const EdgeInsets.symmetric(horizontal: 0.04);
  final head = const EdgeInsets.only(left: 0.04, right: 0.04, top: 0.01);
  final foot = const EdgeInsets.only(left: 0.04, right: 0.04, bottom: 0.01);
}
